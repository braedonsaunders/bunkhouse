name: CodeFlow Card

on:
  push:
    branches: [main]
  pull_request:
    types: [closed]
  workflow_dispatch:

jobs:
  card:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
      - uses: braedonsaunders/codeflow/card@v1
        with:
          accent: amber
          show-grade: false
          show-score: false
          receipts: false

